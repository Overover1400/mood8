<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Required for AI Coach, sign-in, and reflection generation. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Local notifications on Android 13+. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Haptic feedback through HapticService. -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Keep the device awake long enough to deliver a scheduled
         notification + run the foreground callback. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- flutter_local_notifications: receive boot completed so
         scheduled habit reminders survive a device reboot. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Exact alarms — required for daily-repeating habit reminders
         to fire reliably. flutter_local_notifications' repeating
         (matchDateTimeComponents) only respects the next-day cycle
         when scheduled with exactAllowWhileIdle, and inexact mode
         lets the OS coalesce alarms by 15+ minutes on aggressive
         OEMs (Xiaomi/Samsung/etc.). Android 12+ requires the user to
         grant SCHEDULE_EXACT_ALARM explicitly via system Settings —
         declared here gives the user the OPTION; the app prompts
         them at first-reminder-set time and falls back to inexact
         mode if they decline. USE_EXACT_ALARM is for Android 13+
         apps in the "alarm clock"/calendar category and grants the
         same capability without a user prompt; we declare both. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <application
        android:label="Mood8"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- Mood8 also runs as a standalone Wear OS app (built via the
             wear-build workflow targeting lib/main_wear.dart). Keeping this
             metadata is harmless on phones. -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Deep link return path for Stripe checkout. Stripe's
                 success_url / cancel_url on mobile is mood8://checkout-complete
                 — verified that Stripe-hosted checkout accepts custom-scheme
                 redirects (subscription + payment mode both). Android opens
                 our MainActivity in singleTop mode and app_links forwards the
                 URI to Flutter via onAppLink. -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="mood8" />
            </intent-filter>
        </activity>
        <!-- flutter_local_notifications receivers — required for
             scheduled notifications to fire and for them to survive
             device reboot. Without ScheduledNotificationBootReceiver
             every reminder dies the moment the phone reboots; with it
             the plugin re-arms AlarmManager on BOOT_COMPLETED. -->
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- share_plus uses an implicit Send intent. -->
        <intent>
            <action android:name="android.intent.action.SEND" />
        </intent>
        <!-- url_launcher / mailto: lookups. -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="mailto" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>
</manifest>
