# mood8 — round 3 (2026-07-31): social profile + cleaner challenges

1) FOLLOWERS / FOLLOWING (social graph on profile)
   - Let users follow and be followed by other users.
   - On the profile screen, show a "Followers" count and a "Following" count, each tappable to
     open a list (who follows me / who I follow), with a follow/unfollow button per row and a
     user search to find people to follow.
   - Reuse the existing user-search (ChallengeService.searchUsers) where possible.
   - Backend: add followers/following relationships + endpoints (list followers, list following,
     follow, unfollow) if they don't already exist. Keep it consistent with the existing
     challenge-social backend (mood8-backend).

2) INVITE FRIENDS DURING CHALLENGE CREATION
   - In the "create a challenge" flow, add a step/option to invite friends as part of creating it
     (search by name and/or pick from the people you follow), so invites go out when the challenge
     is created — not only afterward from the challenge detail screen.

3) CHALLENGE SECTION — MAKE IT CLEANER & SIMPLER
   - The Challenge tab currently feels too busy/cluttered. Simplify the UI: reduce visual noise,
     fewer elements per screen, clearer hierarchy, cleaner cards, more whitespace/breathing room.
     Keep it friendly and easy to understand at a glance. Preserve functionality; just declutter.

After the changes: flutter analyze clean, then build the latest APK and web (and deploy web).
