# mood8 — follow-up fixes (round 2, 2026-07-31)

1) WEB APP IS EMPTY. https://mood8.app/app renders a blank/empty page. Investigate and fix
   the web build + deployment: run `flutter build web`, confirm the deployed files are current
   and correctly served, and that the app actually loads (check console errors, base href, routing).

2) FRIENDS FEATURE NOT DISCOVERABLE. The invite/search-users UI (lib/widgets/challenges/
   invite_friends_sheet.dart, ChallengeService.searchUsers) is ONLY reachable from inside a
   challenge detail screen (challenge_detail_screen.dart). A user with no challenges never sees it.
   Make friends/invite discoverable from the Challenges tab itself — e.g. an "Invite friends" /
   "Friends" action that lets the user search by name, follow, and invite even BEFORE creating a
   challenge (or clearly funnel: create challenge -> invite). Goal: the user can find and use the
   friends feature without already being inside a challenge.

3) LAYOUT BUG — empty-state button hidden behind the bottom nav bar. On the Challenges empty
   state ("Be the first to start one"), the "Start a challenge" button is partly UNDER the app's
   bottom navigation bar (Today/Habits/Challenge/Coach/Progress). Add bottom padding = bottom nav
   height + safe-area inset to the scrollable content so the button sits fully above the nav bar.
   Check other empty states/screens for the same overlap.

4) BUILD LATEST. After the fixes, build a fresh Android APK (and web) so it can be installed and
   tested, and report the artifact/run link.
